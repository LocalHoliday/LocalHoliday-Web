import styled from '@emotion/styled'

export default function Home() {
  return (
    <>
      <Content>Local Holiday Web!</Content>
    </>
  )
}
const Content = styled.div`
  font-family: var(--font-primary);
  font-weight: 100;
  font-size: 40px;
`
